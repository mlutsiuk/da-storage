export default defineAppConfig({
  ui: {
    input: {
      defaultVariants: {
        size: 'lg'
      }
    },
    selectMenu: {
      defaultVariants: {
        size: 'lg'
      }
    },
    colors: {
      primary: 'blue',
      gray: 'slate'
    }
  }
})
